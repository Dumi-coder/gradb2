Setup steps:
1) Install PHPMailer via Composer:
   composer require phpmailer/phpmailer
2) Update SMTP credentials in db.php (SMTP_USER, SMTP_PASS, SMTP_FROM).
3) Import database.sql if needed.
4) Open forgot_password.php in the browser.
